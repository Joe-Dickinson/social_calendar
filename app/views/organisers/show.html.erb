<% content_for :sidebar do %>

  <%= render "sidebar" %>

<% end %>

<ul class="nav nav-tabs">
  <li class="active">
    <%= link_to "Events Feed", organiser_path(@organiser)%>
  </li>
  <li><%= link_to "Your Events", hosting_path(@organiser) %></li>
  <li><%= link_to "Similar to You", similar_path(@organiser) %></li>
  <li><%= link_to "Recommended Events", recommended_path(@organiser) %></li>
</ul>

<div class="pagination">
  <%= will_paginate @events, renderer: BootstrapPagination::Rails %>
</div>

<div>
  Welcome <%= @organiser.name %> 
</div>

<br>

<h2> All Events </h2>

<%= render "event" %>

<div class="end-padding"> </div>