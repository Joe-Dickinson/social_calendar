<%= form_for @organiser, :url => organiser_path(@organiser) do |f| %>
  <fieldset>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </fieldset>
  <fieldset class="button">
    <%= f.submit "Save" %>
  </fieldset>
  <% end %>
